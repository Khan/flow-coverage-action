# AUTOGENERATED by action-preprocessor.js from ../.github/workflow-templates/main.yml

name: Run on itself!
'on':
  - pull_request
jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        name: '▶️ Setup checkout: '
      - run: yarn
        name: '▶️ Setup yarn: '
      - id: flow-coverage
        uses: Khan/flow-coverage-action@master
        env:
          GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}'
      - id: flow
        run: yarn flow
