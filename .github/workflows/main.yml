# AUTOGENERATED by action-preprocessor.js from flow-coverage-action/.github/workflow-templates/main.yml

name: Run on itself!
'on':
  - pull_request
jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - run: yarn
        name: '▶️ Setup yarn: '
      - id: flow-coverage
        uses: Khan/flow-coverage-action@refactor
        env:
          GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}'
      - id: flow
        run: yarn flow
